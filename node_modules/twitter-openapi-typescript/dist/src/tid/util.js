"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.floatToHex = floatToHex;
exports.isOdd = isOdd;
exports.base64Encode = base64Encode;
function floatToHex(x) {
    var result = [];
    var quotient = Math.floor(x);
    var fraction = x - quotient;
    while (quotient > 0) {
        var div = Math.floor(quotient / 16);
        var remainder = quotient - div * 16;
        if (remainder > 9) {
            result.unshift(String.fromCharCode(remainder + 55));
        }
        else {
            result.unshift(remainder.toString());
        }
        quotient = div;
    }
    if (fraction === 0) {
        return result.join('');
    }
    result.push('.');
    var count = 0;
    while (fraction > 0 && count < 10) {
        fraction *= 16;
        var integer = Math.floor(fraction);
        fraction -= integer;
        if (integer > 9) {
            result.push(String.fromCharCode(integer + 55));
        }
        else {
            result.push(integer.toString());
        }
        count += 1;
    }
    return result.join('');
}
function isOdd(num) {
    return num % 2 ? -1.0 : 0.0;
}
function base64Encode(data) {
    if (typeof data === 'string') {
        data = Buffer.from(data, 'utf-8');
    }
    return data.toString('base64');
}
//# sourceMappingURL=util.js.map