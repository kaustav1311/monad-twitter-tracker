export declare class ClientTransaction {
    static DEFAULT_KEYWORD: string;
    static ADDITIONAL_RANDOM_NUMBER: number;
    static DEFAULT_ROW_INDEX: number | null;
    static DEFAULT_KEY_BYTES_INDICES: number[] | null;
    homePageResponse: string;
    key: string;
    keyBytes: number[];
    animationKey: string;
    constructor(homePageResponse: string);
    init(): Promise<void>;
    validateResponse(response: string): string;
    getIndices(homePageResponse: string): Promise<{
        DEFAULT_ROW_INDEX: number;
        DEFAULT_KEY_BYTES_INDICES: number[];
    }>;
    getKey(response: string): string;
    getKeyBytes(key: string): number[];
    getFrames(response: string): string[];
    get2DArray(keyBytes: number[], response: string, frames?: string[]): number[][];
    solve(value: number, minVal: number, maxVal: number, rounding: boolean): number;
    animate(frames: number[], targetTime: number): string;
    getAnimationKey(keyBytes: number[], response: string): string;
    generateTransactionId(method: string, path: string, response?: string, key?: string, animationKey?: string, timeNow?: number): string;
}
//# sourceMappingURL=transaction.d.ts.map